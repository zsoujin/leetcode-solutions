git add .
git commit -m "Updated push.sh script"
git push

# ./push.sh to push changes to remote repository
# Remember to add a commit message between the quotes
# Example: git commit -m "Added solution for problem XYZ"
# Make sure you have the necessary permissions to push to the repository
# and that you are on the correct branch before running this script